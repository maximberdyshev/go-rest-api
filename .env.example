# Postgres container settings
POSTGRES_DATA=./data
POSTGRES_INIT=./db/init-db.sql

# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=5432
POSTGRES_USER=db_user
POSTGRES_PASSWORD=db_user_password

# App go-rest-api
API_KEY=api_key

# External API
WEBAPI_URL=externalhost:port
WEBAPI_TOKEN=external_token

# Re-define config
ENVIRONMENT=container
